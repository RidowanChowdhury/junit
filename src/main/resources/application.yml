profile:
  name: dev profile
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect : org.hibernate.dialect.PostgresPlusDialect
  datasource:
    url: jdbc:postgresql://127.0.0.1/postgres
    username: ridwan
    password: ridwan