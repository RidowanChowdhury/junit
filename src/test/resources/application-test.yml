test:
  data: test profile
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect : org.hibernate.dialect.PostgresPlusDialect
  datasource:
    url: jdbc:tc:postgresql://localhost/postgres
    username: ridwan
    password: ridwan